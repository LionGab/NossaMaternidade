/**
 * ChatScreen - NathIA Chat (Estilo Claude Mobile)
 *
 * DEPRECATED: Esta é a versão antiga do ChatScreen
 * Use ChatScreen.tsx (versão refatorada com tokens) instead
 *
 * Design clean e funcional inspirado no Claude Mobile
 * - Header minimalista com avatar
 * - Lista de mensagens fluida
 * - Input com botão de voz/texto
 * - Histórico acessível
 *
 * @version 4.0.0 - Redesign Claude Mobile Style
 * @deprecated Use ChatScreen.tsx (refactored version) instead
 */

// Este arquivo foi movido para ChatScreen.old.tsx como backup
// A nova implementação está em ChatScreen.tsx (anteriormente ChatScreenRefactored.tsx)
// Mantido para referência histórica e possível rollback se necessário

export default function ChatScreenOld() {
  throw new Error(
    'ChatScreen.old.tsx is deprecated. Use ChatScreen.tsx (refactored version) instead.'
  );
}
