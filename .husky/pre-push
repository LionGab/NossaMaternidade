#!/bin/bash

echo "Running pre-push validation..."

# Use bun if available, otherwise fallback to npm
if command -v bun > /dev/null 2>&1; then
  PKG_RUNNER="bun"
else
  PKG_RUNNER="npm"
fi

# Run full quality gate
$PKG_RUNNER run quality-gate || exit 1

echo "Ready to push!"
